import Vue from "vue";
// 导入全部组件
// import NutUI from "@nutui/nutui";
// NutUI.install(Vue);
// 按需使用 nutui
import {
  Button,
  Row,
  Col,
  NavBar,
  TextInput,
  Notify,
  Avatar,
  Cell,
  Icon,
  Swiper
} from "@nutui/nutui";
Button.install(Vue);
Row.install(Vue);
Col.install(Vue);
NavBar.install(Vue);
TextInput.install(Vue);
Button.install(Vue);
Notify.install(Vue);
Avatar.install(Vue);
Cell.install(Vue);
Icon.install(Vue);
Swiper.install(Vue);
