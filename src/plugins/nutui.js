import Vue from "vue";
// 导入全部组件
// import NutUI from "@nutui/nutui";
// NutUI.install(Vue);
// 按需使用 nutui
import {Button,Row,Col} from "@nutui/nutui";
Button.install(Vue);
Row.install(Vue);
Col.install(Vue);